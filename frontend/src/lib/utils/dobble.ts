/**
 * Dobble engine stub — full implementation in Phase 2.
 */
export function dobbleEngine(order: number): number[][] {
  throw new Error(`Dobble engine not yet implemented for order ${order} — see Phase 2`);
}
